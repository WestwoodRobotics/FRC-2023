package frc.robot.subsystems.Intake;

import frc.robot.Constants.*;
public class Intake {
    IntakeModule IntakeMotor = new IntakeModule(PortConstants.kIntakeMotorPort, PortConstants.kIntakeMotorEncoderPort); // Need to update 0's to correct values
}
